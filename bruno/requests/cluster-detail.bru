meta {
  name: ClusterDetail
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}
  body: json
}

headers {
  content-type: application/json
}

body:json {
  {
    "query": "query ClusterDetail($clusterId: ID!) { cluster(id: $clusterId) { id status responseText channelCount messages { id text createdAt channelId visitorUserId visitorUsername visitorAvatarUrl } } }",
    "variables": {
      "clusterId": "REPLACE_CLUSTER_ID"
    }
  }
}
